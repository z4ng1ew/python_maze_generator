# Основные зависимости (Tkinter — встроен в Python, не нужен в этом списке)
# Для тестов
pytest>=7.0
pytest-cov>=4.0

# Для бонусной части (Часть 4 — клеточный автомат, ускорим numpy)
numpy>=1.24

# Для бонусной части (Часть 6 — веб-интерфейс)
flask>=3.0
